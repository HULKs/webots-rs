[package]
description = "Webots bindings for Rust"
edition = "2021"
homepage = "https://github.com/HULKs/webots-rs"
license = "MIT"
name = "webots"
version = "0.2.0"

[dependencies]
webots-bindings = {path = "webots-bindings", version = "0.2.0"}
